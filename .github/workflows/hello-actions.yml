name: hello-actions

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print info
        run: |
          echo "Repository: ${{ github.repository }}"
          echo "Runner OS: ${{ runner.os }}"